# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

lang: ru
title: Перевод документации Hugin
author: Иван Шихалев
email: hugin-doc-ru@shikhalev.org
description: >-
  Проект перевода документации по редактору панорам Hugin на русский язык
baseurl: ""
url: "https://hugin.shikhalev.org/"

show_excerpts: true
rss: rss

header_pages:
  - tr/hugin.md
  - about.md

# Build settings
markdown: kramdown
kramdown:
  parse_block_html: true
  auto_ids: true

plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag

remote_theme: shikhalev/jekyll-theme

google:
  search: b2790c4aaf6b24d8c

defaults:
  - scope:
      path: "*"
    values:
      hide_last_comments: true

show_donate: false
show_contacts: false
show_feeds: false

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
# exclude:
#   - Gemfile
#   - Gemfile.lock
#   - node_modules
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
